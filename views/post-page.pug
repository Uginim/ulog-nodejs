extends layout

block content
    .post-detail
        h1.post-title #{title}
        - var formatter = new Intl.DateTimeFormat('ko-KR',{ year: 'numeric', month: '2-digit', day: '2-digit', hour: '2-digit', minute: '2-digit' });
        p.text-muted.mb-4  #{formatter.format(post.createdAt)}
        .post-content
            #content-body #{content}

    .comment-section
        h3.mb-4 댓글
        if(user)
            form#form-comment.mb-4
                .form-group
                    textarea#comment-text.form-control(name='comment' rows='4' placeholder='댓글을 입력하세요...')
                button#comment-send.btn.btn-primary(type='button') 댓글 작성
        else
            p.text-muted 댓글을 작성하려면 로그인이 필요합니다.

        #comment-area
            each comment in post.comments
                .comment-item
                    .comment-username #{comment.user.username}
                    .comment-createdat #{formatter.format(comment.createdAt)}
                    .comment-content #{comment.content}
    script.
       

        
    
